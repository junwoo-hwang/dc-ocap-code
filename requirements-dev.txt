# 테스트용. 운영에는 필요 없다.
#
#   pytest tests/                 <- 브라우저 없이 도는 것들
#   playwright install chromium   <- 한 번만. 차트 규칙 검사에 필요하다
#
# playwright 가 없으면 tests/test_report_page.py 는 통째로 건너뛴다. 다만
# 차트 로직은 전부 템플릿의 자바스크립트에 있어서, 그걸 건너뛰면 이상값
# 제거와 배경 솎기는 아무도 안 보게 된다.
-r requirements.txt
pytest==9.1.1
playwright==1.62.0
